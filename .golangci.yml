linters-settings:
    golint:
        min-confidence: 0.1
    goimports:
        local-prefixes: sagikazarmark.dev/mga

linters:
    enable-all: true
    disable:
        - maligned
        - funlen
        - dupl

service:
    golangci-lint-version: 1.19.x
